import { GuideRegistrationForm } from "@/components/auth-forms";

export const metadata = { title: "Become a guide | My South African Guide" };

export default function GuideRegistrationPage() {
  return <GuideRegistrationForm />;
}
