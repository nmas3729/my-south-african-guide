import { TravellerRegistrationForm } from "@/components/auth-forms";

export const metadata = { title: "Create an account | My South African Guide" };

export default function RegisterPage() {
  return <TravellerRegistrationForm />;
}
