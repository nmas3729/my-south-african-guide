"use client";

import Image from "next/image";
import Link from "next/link";
import { motion, useReducedMotion } from "framer-motion";
import { ArrowRight, CalendarDays, Check, ChevronDown, Compass, MapPin, Menu, Search, Star, Users, X } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { contactEmail, formatExperiencePrice, getExperienceFilterOptions, getGuideFilterOptions, getWhatsAppHref, searchExperiences, searchGuides, type Experience, type Guide, type MarketplaceKind } from "@/lib/marketplace";

export function Navbar() {
  const [open, setOpen] = useState(false);
  const [user, setUser] = useState<{ firstName: string | null } | null>(null);
  const [authLoaded, setAuthLoaded] = useState(false);
  useEffect(() => {
    fetch("/api/auth/me", { credentials: "same-origin" }).then((response) => response.json()).then((data: { user?: { firstName: string | null } | null }) => { setUser(data.user ?? null); setAuthLoaded(true); }).catch(() => setAuthLoaded(true));
  }, []);
  const links = [["Explore", "/experiences"], ["Guides", "/guides"], ["Destinations", "/destinations"], ["Transport", "/transport"], ["Become a Guide", "/become-guide"], ["About", "/about"], ["Contact", "/contact"]] as const;
  const authLinks = authLoaded ? (user ? [[user.firstName ? `Account (${user.firstName})` : "Account", "/account"]] : [["Login", "/login"], ["Become a Guide", "/register/guide"]]) : [];
  return <header className="absolute inset-x-0 top-0 z-20 border-b border-white/15 text-white"><div className="mx-auto flex h-20 max-w-[1320px] items-center justify-between px-5 lg:px-8"><Link href="/" className="flex items-center gap-3" aria-label="My South African Guide home"><span className="grid h-9 w-9 place-items-center rounded-full border border-gold/70 text-gold"><Compass size={18} /></span><span className="max-w-[150px] text-[11px] font-semibold uppercase leading-[1.05] tracking-[0.22em]">My South<br />African Guide</span></Link><nav className="hidden items-center gap-5 text-xs font-medium tracking-[0.06em] xl:flex" aria-label="Primary navigation">{links.map(([label, href]) => <Link key={href} className="nav-link" href={href}>{label}</Link>)}{authLinks.map(([label, href]) => <Link key={href} className="nav-link text-gold" href={href}>{label}</Link>)}</nav><Link className="hidden rounded-full border border-white/40 px-5 py-2.5 text-xs font-semibold tracking-[0.08em] transition hover:bg-white hover:text-ink lg:inline-flex" href={user ? "/account" : "/contact"}>{user ? "Account" : "Plan My Journey"}</Link><button type="button" onClick={() => setOpen(!open)} className="rounded p-2 focus-visible:outline focus-visible:outline-2 focus-visible:outline-gold lg:hidden" aria-expanded={open} aria-controls="mobile-navigation" aria-label={open ? "Close menu" : "Open menu"}>{open ? <X /> : <Menu />}</button></div>{open && <nav id="mobile-navigation" className="border-t border-white/15 bg-ink px-6 py-5 lg:hidden" aria-label="Mobile navigation"><div className="flex flex-col gap-1 text-sm">{links.map(([label, href]) => <Link key={href} href={href} onClick={() => setOpen(false)} className="rounded px-2 py-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-gold">{label}</Link>)}{authLinks.map(([label, href]) => <Link key={href} href={href} onClick={() => setOpen(false)} className="rounded px-2 py-3 text-gold focus-visible:outline focus-visible:outline-2 focus-visible:outline-gold">{label}</Link>)}</div></nav>}</header>;
}

export function SearchBox() { return <div className="search-box grid divide-y divide-ink/10 overflow-hidden rounded-[6px] bg-cream md:grid-cols-[1.25fr_1fr_1fr_0.9fr_auto] md:divide-x md:divide-y-0"><div className="search-field"><MapPin size={17} /><span><small>Where to?</small><strong>South Africa</strong></span><ChevronDown className="ml-auto" size={16} /></div><div className="search-field"><CalendarDays size={17} /><span><small>When</small><strong>Choose dates</strong></span></div><div className="search-field"><Compass size={17} /><span><small>Experience</small><strong>Any experience</strong></span><ChevronDown className="ml-auto" size={16} /></div><div className="search-field"><Users size={17} /><span><small>Travellers</small><strong>2 travellers</strong></span></div><button className="grid min-h-16 place-items-center bg-red px-7 text-sm font-semibold uppercase tracking-[0.12em] text-white transition hover:bg-[#961a1f]" aria-label="Search experiences"><Search size={20} /></button></div>; }
export function TrustBadge({ children }: { children: React.ReactNode }) { return <span className="inline-flex items-center gap-1.5 rounded-full bg-green/10 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.12em] text-green"><Check size={13} strokeWidth={3} />{children}</span>; }
export function ExperienceCard({ experience }: { experience: Experience }) { return <article className="group h-full overflow-hidden rounded-[4px] border border-ink/10 bg-white shadow-[0_10px_28px_rgba(7,26,43,0.04)] transition duration-300 hover:-translate-y-1 hover:shadow-[0_18px_38px_rgba(7,26,43,0.09)]"><Link href={`/experiences/${experience.slug}`} className="block h-full"><div className="relative aspect-[1.15] overflow-hidden"><Image src={experience.image} alt={experience.title} fill sizes="(max-width: 768px) 90vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105" /><span className="absolute left-4 top-4 rounded-full bg-cream/90 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.1em] text-ink">{experience.category}</span></div><div className="p-5"><div className="mb-2 flex items-center justify-between text-xs text-ink/60"><span className="flex items-center gap-1"><MapPin size={13} />{experience.location}</span><span className="flex items-center gap-1 font-semibold text-ink"><Star size={13} fill="currentColor" className="text-gold" />{experience.rating} <span className="font-normal text-ink/45">({experience.reviewCount})</span></span></div><h3 className="font-serif text-[23px] leading-tight text-ink">{experience.title}</h3><div className="mt-5 flex items-end justify-between border-t border-ink/10 pt-4"><span className="text-xs text-ink/55">{experience.duration}<br /><strong className="mt-1 inline-block text-sm text-ink">{formatExperiencePrice(experience)}</strong></span><span className="flex items-center gap-1 text-xs font-bold uppercase tracking-[0.1em] text-red">View experience <ArrowRight size={14} /></span></div></div></Link></article>; }
export function GuideCard({ guide }: { guide: Guide }) { return <article className="group h-full overflow-hidden rounded-[4px] border border-ink/10 bg-white p-3 shadow-[0_10px_25px_rgba(7,26,43,0.04)] transition duration-300 hover:-translate-y-1 hover:shadow-[0_18px_38px_rgba(7,26,43,0.08)]"><Link href={`/guides/${guide.slug}`} className="block h-full"><div className="relative aspect-[1.05] overflow-hidden"><Image src={guide.profileImage} alt={guide.name} fill sizes="(max-width: 768px) 90vw, 28vw" className="object-cover transition duration-700 group-hover:scale-105" /><span className="absolute bottom-3 left-3"><TrustBadge>{guide.verified ? "Verified" : "Profile under review"}</TrustBadge></span></div><div className="px-2 pb-2 pt-4"><div className="flex items-center justify-between"><h3 className="font-serif text-[23px] text-ink">{guide.name}</h3><span className="flex items-center gap-1 text-xs font-bold"><Star size={13} fill="currentColor" className="text-gold" />{guide.rating}</span></div><p className="mt-1 text-xs text-ink/55">{guide.location}</p><p className="mt-4 text-xs leading-relaxed text-ink/70">{guide.specialities.join(" · ")}<br />{guide.languages.join(" · ")}</p><span className="mt-5 flex items-center justify-between border-t border-ink/10 pt-3 text-xs font-bold uppercase tracking-[0.1em] text-red">View profile <ArrowRight size={14} /></span></div></Link></article>; }

export function MarketplaceResults({ kind }: { kind: MarketplaceKind }) {
  const [query, setQuery] = useState("");
  const [province, setProvince] = useState("");
  const [language, setLanguage] = useState("");
  const [speciality, setSpeciality] = useState("");
  const [destination, setDestination] = useState("");
  const [category, setCategory] = useState("");
  const [duration, setDuration] = useState("");
  const [experienceType, setExperienceType] = useState("");
  const [maxPrice, setMaxPrice] = useState("");
  const guideOptions = getGuideFilterOptions();
  const experienceOptions = getExperienceFilterOptions();
  const items = useMemo(() => kind === "guides" ? searchGuides({ query, province, language, speciality }) : searchExperiences({ query, destination, category, duration, experienceType, maxPrice: maxPrice ? Number(maxPrice) : undefined }), [kind, query, province, language, speciality, destination, category, duration, experienceType, maxPrice]);
  return <div><div className="mb-10 grid gap-3 rounded border border-ink/10 bg-white p-4 md:grid-cols-2 lg:grid-cols-4"><label className="flex min-h-11 items-center gap-3 border-b border-ink/10 lg:col-span-2"><Search size={18} className="text-red" /><span className="sr-only">Search {kind}</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder={`Search ${kind}...`} className="w-full bg-transparent py-2 text-sm outline-none focus-visible:ring-2 focus-visible:ring-gold" /></label>{kind === "guides" ? <><select aria-label="Filter by province" value={province} onChange={(event) => setProvince(event.target.value)} className="min-h-11 border border-ink/10 bg-cream px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-gold"><option value="">All provinces</option>{guideOptions.provinces.map((option) => <option key={option}>{option}</option>)}</select><select aria-label="Filter by language" value={language} onChange={(event) => setLanguage(event.target.value)} className="min-h-11 border border-ink/10 bg-cream px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-gold"><option value="">All languages</option>{guideOptions.languages.map((option) => <option key={option}>{option}</option>)}</select><select aria-label="Filter by speciality" value={speciality} onChange={(event) => setSpeciality(event.target.value)} className="min-h-11 border border-ink/10 bg-cream px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-gold"><option value="">All specialities</option>{guideOptions.specialities.map((option) => <option key={option}>{option}</option>)}</select></> : <><select aria-label="Filter by destination" value={destination} onChange={(event) => setDestination(event.target.value)} className="min-h-11 border border-ink/10 bg-cream px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-gold"><option value="">All destinations</option>{experienceOptions.destinations.map((option) => <option key={option}>{option}</option>)}</select><select aria-label="Filter by category" value={category} onChange={(event) => setCategory(event.target.value)} className="min-h-11 border border-ink/10 bg-cream px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-gold"><option value="">All categories</option>{experienceOptions.categories.map((option) => <option key={option}>{option}</option>)}</select><select aria-label="Filter by duration" value={duration} onChange={(event) => setDuration(event.target.value)} className="min-h-11 border border-ink/10 bg-cream px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-gold"><option value="">Any duration</option>{experienceOptions.durations.map((option) => <option key={option}>{option}</option>)}</select><select aria-label="Filter by experience type" value={experienceType} onChange={(event) => setExperienceType(event.target.value)} className="min-h-11 border border-ink/10 bg-cream px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-gold"><option value="">All types</option>{experienceOptions.experienceTypes.map((option) => <option key={option}>{option}</option>)}</select><select aria-label="Filter by maximum price" value={maxPrice} onChange={(event) => setMaxPrice(event.target.value)} className="min-h-11 border border-ink/10 bg-cream px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-gold"><option value="">Any price</option><option value="3000">Up to R3,000</option><option value="4000">Up to R4,000</option></select></>}</div>{items.length ? <div className="grid gap-5 md:grid-cols-3">{items.map((item) => kind === "guides" ? <GuideCard key={item.slug} guide={item as Guide} /> : <ExperienceCard key={item.slug} experience={item as Experience} />)}</div> : <div className="py-16 text-center"><p className="font-serif text-3xl text-ink/60">No matches yet.</p><p className="mt-2 text-sm text-ink/50">Try a different search or clear a filter.</p></div>}</div>;
}

export function RequestForm({ title = "Request this journey", subject = "Journey enquiry" }: { title?: string; subject?: string }) {
  const [message, setMessage] = useState("");
  function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const body = `Name: ${data.get("name")}\nEmail: ${data.get("email")}\nDates: ${data.get("dates") || "Not specified"}\n\nMessage:\n${data.get("plans")}`; if (!contactEmail) { setMessage("Email enquiries are not configured yet. Please use WhatsApp when a contact number is available."); return; } window.location.href = `mailto:${contactEmail}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`; setMessage("Your email app should open with this enquiry ready to send. No booking or payment has been made."); }
  const whatsappHref = getWhatsAppHref(subject);
  return <form onSubmit={submit} className="space-y-4 rounded bg-white p-6 shadow-lg"><h3 className="font-serif text-3xl text-ink">{title}</h3><label className="sr-only" htmlFor="name">Your name</label><input id="name" name="name" required placeholder="Your name" className="w-full border-b border-ink/20 py-3 text-sm outline-none focus-visible:border-gold" /><label className="sr-only" htmlFor="email">Email address</label><input id="email" name="email" required type="email" placeholder="Email address" className="w-full border-b border-ink/20 py-3 text-sm outline-none focus-visible:border-gold" /><label className="sr-only" htmlFor="dates">Preferred dates</label><input id="dates" name="dates" placeholder="Preferred dates (optional)" className="w-full border-b border-ink/20 py-3 text-sm outline-none focus-visible:border-gold" /><label className="sr-only" htmlFor="plans">Tell us about your plans</label><textarea id="plans" name="plans" required placeholder="Tell us a little about your plans" rows={3} className="w-full resize-none border-b border-ink/20 py-3 text-sm outline-none focus-visible:border-gold" /><button type="submit" className="w-full bg-red px-5 py-4 text-xs font-bold uppercase tracking-[.14em] text-white transition hover:bg-[#961a1f] focus-visible:outline focus-visible:outline-2 focus-visible:outline-gold">Open enquiry email</button>{whatsappHref && <a href={whatsappHref} target="_blank" rel="noreferrer" className="block w-full border border-green px-5 py-4 text-center text-xs font-bold uppercase tracking-[.14em] text-green">Continue on WhatsApp</a>}{message && <p role="status" className="text-xs leading-relaxed text-green">{message}</p>}</form>;
}
export function SectionHeading({ eyebrow, title, copy, action, href = "/experiences" }: { eyebrow: string; title: string; copy?: string; action?: string; href?: string }) { return <div className="mb-10 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="eyebrow">{eyebrow}</p><h2 className="mt-3 max-w-2xl font-serif text-4xl leading-[1.04] text-ink md:text-5xl">{title}</h2>{copy && <p className="mt-4 max-w-xl text-sm leading-relaxed text-ink/60">{copy}</p>}</div>{action && <Link href={href} className="flex shrink-0 items-center gap-2 text-xs font-bold uppercase tracking-[0.12em] text-red">{action}<ArrowRight size={15} /></Link>}</div>; }
export function FadeIn({ children, className = "" }: { children: React.ReactNode; className?: string }) {
  const shouldReduceMotion = useReducedMotion();
  return <motion.div initial={shouldReduceMotion ? false : { opacity: 0, y: 18 }} whileInView={shouldReduceMotion ? undefined : { opacity: 1, y: 0 }} viewport={{ once: true, margin: "-80px" }} transition={{ duration: 0.6 }} className={className}>{children}</motion.div>;
}
export function Footer() { const groups: Array<[string, Array<[string, string]>]> = [["Explore", [["Experiences", "/experiences"], ["Guides", "/guides"], ["Destinations", "/destinations"], ["Transport", "/transport"]]], ["Company", [["About us", "/about"], ["Become a guide", "/become-guide"], ["Contact", "/contact"]]]]; return <footer className="bg-ink px-5 pb-8 pt-16 text-white lg:px-8"><div className="mx-auto max-w-[1320px]"><div className="grid gap-12 border-b border-white/15 pb-14 md:grid-cols-[2fr_1fr_1fr]"><div><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full border border-gold/70 text-gold"><Compass size={18} /></span><span className="text-[11px] font-semibold uppercase leading-[1.05] tracking-[0.22em]">My South<br />African Guide</span></div><p className="mt-6 max-w-xs font-serif text-2xl leading-tight text-white/85">Every journey has a story.<br />Let us show you yours.</p></div>{groups.map(([heading, items]) => <div key={heading}><p className="mb-5 text-[10px] font-bold uppercase tracking-[0.18em] text-gold">{heading}</p><div className="flex flex-col gap-3 text-sm text-white/65">{items.map(([label, href]) => <Link key={href} href={href} className="transition hover:text-white">{label}</Link>)}</div></div>)}</div><div className="flex flex-col justify-between gap-3 pt-6 text-[10px] uppercase tracking-[0.12em] text-white/40 md:flex-row"><span>© 2026 My South African Guide</span><span>Made with care in South Africa</span></div><p className="pt-4 text-[10px] uppercase tracking-[0.12em] text-white/40"><span>Site Design &amp; hosted by <a href="https://sihleb.co.za" target="_blank" rel="noopener noreferrer" className="text-white/75 underline-offset-4 transition hover:text-gold hover:underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-gold focus-visible:outline-offset-2">SihleB Digital Studio</a></span></p></div></footer>; }